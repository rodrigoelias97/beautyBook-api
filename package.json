{
  "name": "beautybook-api",
  "version": "1.0.0",
  "description": "API REST simples do BeautyBook com Express e banco em memoria.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "mocha --file test/helpers/test-setup.js \"test/**/*.test.js\"",
    "test:report": "mocha --file test/helpers/test-setup.js \"test/**/*.test.js\" --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=resultado-testes,quiet=true"
  },
  "dependencies": {
    "express": "^4.21.2",
    "swagger-ui-express": "^5.0.1",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "chai": "^5.2.0",
    "mocha": "^11.7.4",
    "mochawesome": "^7.1.3",
    "supertest": "^7.1.1"
  }
}
